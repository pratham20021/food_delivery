# Maven
food-delivery/target/
target/
food-delivery/.mvn/wrapper/maven-wrapper.jar

# Terraform — NEVER commit state or secrets
terraform/.terraform/
terraform/.terraform.lock.hcl
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
terraform/tfplan
terraform/tfplan.txt
terraform/terraform.tfvars
terraform/*.auto.tfvars
terraform/modules/lambda/zips/

# Lambda layer installed packages — installed by pipeline, not committed
lambda/layers/db_utils/python/
lambda/layers/aws_clients/python/

# Environment files — NEVER commit real credentials
.env
*.env
!.env.example
/etc/food-delivery.env

# IDE
.idea/
*.iml
.vscode/
*.swp

# Docker
.dockerignore

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Jenkins
jenkins/jenkins_home/
